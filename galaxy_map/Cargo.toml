[package]
name = "galaxy_map"
description = "A crate contains code related to galaxy map (or refer as L3 map in the code)."
version = { workspace = true }
edition = { workspace = true }
authors = { workspace = true }

[dependencies]
bevy = { workspace = true }
utils = { path = "../utils" }
